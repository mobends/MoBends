pluginManagement {
    repositories {
        maven { url = uri("https://repo1.maven.org/maven2") }
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "mobends-animation-lab"
