package goblinbob.mobends.core;

import java.util.logging.Logger;

/** LAB SHIM. Only the logger is needed by the animation code. */
public class Core
{
    public static final Logger LOG = Logger.getLogger("mobends-core");
}
